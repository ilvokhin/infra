- name: Install dev packages
  ansible.builtin.package:
    name:
      - gcc
      - clang
      - python
      - perf
      - autoconf
      - make
      - cmake
      - benchmark
      - gtest
      - ninja
      - ctags
      - jq
      - tree
      # Linux man pages.
      - man-pages
    state: present
